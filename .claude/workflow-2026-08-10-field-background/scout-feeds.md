{
  "findings": "## 1. `MicLevelFeed` \u2014 exact declaration\n\nLives in `/Users/xiafawu/Developer/Nota/macos/Nota/UI/RecordingPane.swift:223-262` (NOT in RecordingAccent.swift, and not in LiveMeetingSession.swift):\n\n```swift\n/// The microphone level, published on an object **only the meter observes**.\n///\n/// It is deliberately not a `@Published` property of `LiveMeetingSession`: that\n/// object is observed by `ContentView` and `LiveMeetingView`, and a 45 Hz feed\n/// on it re-renders the window. Held by the session as a plain `let`, so\n/// mutating it never touches the session's own `objectWillChange`.\n@MainActor\nfinal class MicLevelFeed: ObservableObject {\n  @Published private(set) var level: Float\n\n  private var lastPublishedAt: TimeInterval = -.greatestFiniteMagnitude\n\n  init(level: Float = 0) {\n    self.level = level\n  }\n\n  /// Publish if `MeterPublishGate` allows it. `now` is injected so the gate is\n  /// testable without waiting out a real 66 ms.\n  func publish(_ level: Float, now: TimeInterval = ProcessInfo.processInfo.systemUptime) {\n    guard\n      MeterPublishGate.shouldPublish(\n        new: level,\n        last: self.level,\n        now: now,\n        lastPublishedAt: lastPublishedAt\n      )\n    else { return }\n    self.level = level\n    lastPublishedAt = now\n  }\n\n  /// Silence, immediately and ungated. Capture ending is the one level change\n  /// that may not wait for a tick: a meter frozen at the last thing it heard is\n  /// a meter claiming a live session.\n  func silence(now: TimeInterval = ProcessInfo.processInfo.systemUptime) {\n    guard level != 0 else { return }\n    level = 0\n    lastPublishedAt = now\n  }\n}\n```\n\nSo: `@MainActor final class`, **classic `ObservableObject` + `@Published private(set)`** (not `@Observable`), injectable `now` for testability, and an ungated `silence()` escape.\n\nHeld on the session as a plain `let` \u2014 `/Users/xiafawu/Developer/Nota/macos/Nota/Dictation/LiveMeetingSession.swift:111`:\n\n```swift\n  /// A plain `let` holding its **own** observable object, deliberately, and not\n  /// a `@Published Float` on this one. The tap delivers ~45 buffers a second\n  /// and this session is observed by `ContentView` and `LiveMeetingView`, so a\n  /// level on it invalidated the whole window body \u2014 toolbar, drawer overlay\n  /// and the entire transcript \u2014 45 times a second. `MeterPublishGate` throttles\n  /// the writes on top of that; see it for the arithmetic.\n  let level = MicLevelFeed()\n```\n(surrounding `@Published private(set) var state/segments/partialText/elapsed` at lines 90-93 are the properties that DO invalidate the window \u2014 the level deliberately is not one of them.)\n\n## 2. `MeterPublishGate` \u2014 verbatim (`RecordingPane.swift:202-221`)\n\n```swift\nenum MeterPublishGate {\n  /// 66 ms \u2014 the HUD's RMS tick, i.e. ~15 Hz.\n  static let minInterval: TimeInterval = 0.066\n  /// Below this the tallest bar moves under a point.\n  static let minDelta: Float = 0.02\n  /// Past this, any difference publishes: convergence beats economy.\n  static let maxHold: TimeInterval = 0.5\n\n  static func shouldPublish(\n    new: Float,\n    last: Float,\n    now: TimeInterval,\n    lastPublishedAt: TimeInterval\n  ) -> Bool {\n    guard new != last else { return false }\n    let since = now - lastPublishedAt\n    guard since >= minInterval else { return false }\n    return abs(new - last) >= minDelta || since >= maxHold\n  }\n}\n```\n\nLogic in words: identical value \u2192 never publish; `< 66 ms` since last publish \u2192 never publish (hard rate ceiling ~15 Hz); after 66 ms publish only if the delta is visible (`>= 0.02`) **or** 500 ms have elapsed (the escape that prevents a sub-threshold decay-to-silence wedging the meter at a loud reading).\n\nThe doc comment above it (lines 176-201) is the rationale: `MicCapture` taps 1024 frames, ~21 ms, ~45/sec; unconditional `@Published` writes on the session invalidated the whole window.\n\n## 3. Who observes the feed, and how\n\n`SessionMeterFeedView` \u2014 `RecordingPane.swift:264-276`, `@ObservedObject`:\n\n```swift\n/// `SessionMeter` bound to the live feed.\n///\n/// This wrapper is the entire point of the split: `@ObservedObject` **here**\n/// means a level tick re-runs *this* body and nothing above it \u2014 not the\n/// column, not the transcript, not the window.\nstruct SessionMeterFeedView: View {\n  @ObservedObject var feed: MicLevelFeed\n  var variant: SessionMeterMetrics.Variant\n\n  var body: some View {\n    SessionMeter(level: feed.level, variant: variant)\n  }\n}\n```\n\nEverything upstream passes the **object**, never the `Float`: `SessionColumnView.level: MicLevelFeed` (`:736`, comment: \"Passed rather than a `Float` so the level's ~15 Hz feed is observed by `SessionMeterFeedView` alone\"), `SessionColumnContent.level` (`:789`), `SessionStripView.level` (`:839`). Only two call sites construct the observer: `RecordingPane.swift:804` and `:851`, both `SessionMeterFeedView(feed: level, variant: RecordingPaneLayout.meterVariant(form))`. `SessionMeter` itself (in RecordingAccent.swift) takes a plain `let level: Float` and is therefore never the observer.\n\nProducer side (`LiveMeetingSession.swift:722-728`, called from the `onPCMBuffer` closures at `:220-226` and `:535-540` inside `Task { @MainActor in \u2026 }`):\n\n```swift\n  private func publishLevel() {\n    if LiveMeetingSession.meterFollowsMicrophone(state) {\n      level.publish(capture.rmsLevel)\n    } else {\n      level.silence()\n    }\n  }\n```\nplus `stopCapture()` (`:701-705`) ending in `level.silence()`. `MicCapture.rmsLevel` is itself `@Published private(set) var rmsLevel: Float = 0` on `final class MicCapture: ObservableObject` (`MicCapture.swift:5,7`), written at `:199` \u2014 but no view observes `MicCapture`; the session reads it imperatively. Smoothing (fast-attack/slow-release envelope, \u221250 dB floor) is `MicCapture.meterLevel(rms:previous:)` at `MicCapture.swift:15-23`.\n\n## 4. Reduce Motion\n\nRead via the SwiftUI environment key **`\\.accessibilityReduceMotion`**, in the leaf views only, and routed through the pure `RecordingMotion` enum. `/Users/xiafawu/Developer/Nota/macos/Nota/UI/RecordingAccent.swift:21-34`:\n\n```swift\nenum RecordingMotion {\n  /// Never nil, at either setting: the meter always animates.\n  static func meterAnimation(reduceMotion: Bool) -> Animation? {\n    reduceMotion\n      ? .linear(duration: 0.10)\n      : .spring(response: 0.28, dampingFraction: 0.55)\n  }\n\n  /// Nil under Reduce Motion: the ring holds at a steady scale and opacity.\n  static func ringAnimation(reduceMotion: Bool) -> Animation? {\n    guard !reduceMotion else { return nil }\n    return .easeInOut(duration: SessionRingMetrics.cycle / 2).repeatForever(autoreverses: true)\n  }\n}\n```\n\nUsage \u2014 `SessionMeter` (`:131`, `:145`):\n```swift\n  @Environment(\\.accessibilityReduceMotion) private var reduceMotion\n  \u2026\n    .animation(RecordingMotion.meterAnimation(reduceMotion: reduceMotion), value: level)\n```\n`SessionRing` (`:193`, `:212-216`):\n```swift\n  @Environment(\\.accessibilityReduceMotion) private var reduceMotion\n  @State private var atPeak = false\n  \u2026\n  private func startBreathing() {\n    guard SessionRingMetrics.breathes(reduceMotion: reduceMotion) else { return }\n    guard let animation = RecordingMotion.ringAnimation(reduceMotion: reduceMotion) else { return }\n    withAnimation(animation) { atPeak = true }\n  }\n```\n`SessionRingMetrics.breathes(reduceMotion:) -> Bool { !reduceMotion }` (`:183`). Also declared in `ContentView.swift:22` (`@Environment(\\.accessibilityReduceMotion) private var reduceMotion`).\n\nKey rule pinned in the source: `reduceMotion` is deliberately NOT a parameter of `SessionMeterMetrics.barHeights(level:variant:)` (`:98-101`) \u2014 heights are what the mic is doing; only the curve between readings is a motion preference. Tests: `RecordingAccentTests.swift:19-26`, `RecordingPaneTests.swift:196-197` assert `ringAnimation(reduceMotion: true) == nil` and `meterAnimation(reduceMotion: true) != nil`.\n\n## 5. Existing repeating timers / display links\n\nThree, no `CADisplayLink` anywhere.\n\na) **`TimelineView(.animation(...))` \u2014 the ONLY per-frame animation driver in the app**, and it is the dictation HUD pill meter, not the recording pane. `/Users/xiafawu/Developer/Nota/macos/Nota/Dictation/DictationHUDPanel.swift:843-856`:\n```swift\n      // The timeline phase adds a slow low-amplitude breathing so the meter\n      // never freezes at steady input \u2014 a still meter reads as dead.\n      TimelineView(.animation(minimumInterval: 1.0 / 30.0)) { timeline in\n        let phase = timeline.date.timeIntervalSinceReferenceDate\n        HStack(spacing: 3) {\n          ForEach(0..<Self.barCount, id: \\.self) { i in\n            Capsule()\n              .fill(.primary)\n              .frame(width: 3.5, height: barHeight(for: i, phase: phase))\n          }\n        }\n        .frame(height: HUDPillMetrics.meterHeight)\n        .animation(.spring(response: 0.28, dampingFraction: 0.55), value: level)\n      }\n```\nwith `barHeight(for:phase:)` at `:859-870` mixing a per-bar `wobble` and an idle `breathe = 0.05 + 0.04 * sin(phase * 2.1 + Double(index) * 0.8)`. Note this is inside a floating NSPanel whose hierarchy is tiny \u2014 the recording pane deliberately does NOT use a TimelineView.\n\nb) **`Timer.scheduledTimer`** \u2014 `/Users/xiafawu/Developer/Nota/macos/Nota/App/BackgroundProcessing.swift:209-221`, a job-list \"updated Ns ago\" ticker, started/stopped by `syncTicker()` only while `isBusy`, with `timer.tolerance = Self.tickInterval / 4` and `RunLoop.main.add(timer, forMode: .common)`.\n\nc) **The elapsed clock is a `Task` sleep loop, not a Timer** \u2014 `LiveMeetingSession.swift:797-806`:\n```swift\n  private func startElapsedTicker() {\n    elapsed = 0\n    elapsedTask = Task { @MainActor [weak self] in\n      while !Task.isCancelled {\n        try? await Task.sleep(nanoseconds: 250_000_000)\n        guard let self, !Task.isCancelled, let startedAt = self.startedAt else { return }\n        self.elapsed = Date().timeIntervalSince(startedAt)\n      }\n    }\n  }\n```\nThis writes `@Published elapsed` on the session at 4 Hz \u2014 i.e. it DOES invalidate the window body, which is why the transcript row model is memoized against whole-seconds of `elapsed` (`LiveTranscriptRowCache`, per CLAUDE.md). Any new high-rate feed should follow the `MicLevelFeed` pattern instead: a separate `@MainActor final class \u2026 ObservableObject` + a gate + a leaf `@ObservedObject` wrapper view.\n"
}