{
  "findings": "## 0. Correction to the task premise\n\n`testTheIdlePaneDrawsNoEmber` is **not** in `RecordingAccentTests.swift`. It lives in\n`/Users/xiafawu/Developer/Nota/macos/Nota/UI/Tests/RecordingPaneTests.swift:725`, and so does the\n`RenderProbe` helper it uses (same file, `:765`). `RecordingAccentTests.swift` has no pixel probe at\nall \u2014 it only measures `NSHostingView.fittingSize`.\n\n## 1. The test and its helper (both in `/Users/xiafawu/Developer/Nota/macos/Nota/UI/Tests/RecordingPaneTests.swift`)\n\nTest, verbatim (`:719-745`), inside `@MainActor final class RecordingPaneTests: XCTestCase`:\n\n```swift\n  /// The idle pane draws **no ember**. It is the state every owner sees before\n  /// every recording, so it is the state that teaches them what the colour\n  /// means \u2014 and `CraftTokens.ember(_:)` means exactly one thing, that the\n  /// microphone is open. Rendered and scanned rather than asserted about a\n  /// constant, because \"there is no ember on screen\" is a claim about pixels.\n  func testTheIdlePaneDrawsNoEmber() {\n    let idle = LiveMeetingView(session: LiveMeetingSession(), onStart: {}, onStop: {})\n    guard\n      let bitmap = RenderProbe.bitmap(\n        idle.environment(\\.colorScheme, .light),\n        size: CGSize(width: 600, height: 420)\n      )\n    else { return XCTFail(\"the hosting view produced no bitmap\") }\n\n    XCTAssertEqual(\n      RenderProbe.emberPixels(bitmap, scheme: .light),\n      0,\n      \"the idle pane is drawing the recording accent over a closed microphone\"\n    )\n  }\n\n  /// \u2026and the probe can see ember when there is ember, or the test above would\n  /// pass against a blank canvas.\n  func testTheProbeSeesTheEmberItIsLookingFor() {\n    guard\n      let bitmap = RenderProbe.bitmap(\n        ZStack {\n          Color.white\n          SessionRing(diameter: 80, lineWidth: 6)\n        }\n        .environment(\\.colorScheme, .light),\n        size: CGSize(width: 200, height: 200)\n      )\n    else { return XCTFail(\"the hosting view produced no bitmap\") }\n    XCTAssertGreaterThan(RenderProbe.emberPixels(bitmap, scheme: .light), 0)\n  }\n}\n```\n\nNote the positive-control test immediately after it \u2014 the pattern is \"assert absence, plus a second\ntest proving the probe can see the thing\".\n\nThe helper, verbatim and complete (same file, `:758-816`, top-level after the class):\n\n```swift\n// MARK: - Rendering probe\n\n/// Renders a view into a bitmap so a claim about **pixels** can be asserted.\n///\n/// Two of this suite's claims are of that kind and neither could be made about\n/// a constant: \"the idle pane draws no ember\" and \"Stop is drawn identically\n/// with and without Reduce Transparency\".\n@MainActor\nenum RenderProbe {\n  static func bitmap<V: View>(_ view: V, size: CGSize) -> NSBitmapImageRep? {\n    let host = NSHostingView(rootView: view.frame(width: size.width, height: size.height))\n    host.frame = CGRect(origin: .zero, size: size)\n    host.layoutSubtreeIfNeeded()\n    guard let rep = host.bitmapImageRepForCachingDisplay(in: host.bounds) else { return nil }\n    host.cacheDisplay(in: host.bounds, to: rep)\n    return rep\n  }\n\n  /// How many sampled pixels are the recording accent.\n  ///\n  /// Matched on **hue and saturation**, not on RGB distance: the ring strokes\n  /// at 55\u201390% opacity, so an ember pixel over a light ground is a long way\n  /// from `#d1662a` in RGB while being unmistakably the same colour. The\n  /// saturation floor is what keeps the ring's 10% interior wash \u2014 a glow, not\n  /// a fill \u2014 and ordinary near-neutral chrome out of the count.\n  static func emberPixels(_ rep: NSBitmapImageRep, scheme: ColorScheme) -> Int {\n    guard let target = NSColor(CraftTokens.ember(scheme)).usingColorSpace(.sRGB) else { return 0 }\n    let targetHue = target.hueComponent\n    var count = 0\n    for x in stride(from: 0, to: rep.pixelsWide, by: 2) {\n      for y in stride(from: 0, to: rep.pixelsHigh, by: 2) {\n        guard let pixel = rep.colorAt(x: x, y: y)?.usingColorSpace(.sRGB) else { continue }\n        guard pixel.alphaComponent > 0.5, pixel.saturationComponent > 0.15 else { continue }\n        let dh = abs(pixel.hueComponent - targetHue)\n        if min(dh, 1 - dh) < 0.04 { count += 1 }\n      }\n    }\n    return count\n  }\n\n  /// The colour at one point, in the bitmap's own pixel space (which is the\n  /// backing scale times the point, on a Retina host).\n  static func color(_ rep: NSBitmapImageRep, at point: CGPoint) -> NSColor? {\n    let scaleX = CGFloat(rep.pixelsWide) / max(rep.size.width, 1)\n    let scaleY = CGFloat(rep.pixelsHigh) / max(rep.size.height, 1)\n    let x = min(max(Int(point.x * scaleX), 0), rep.pixelsWide - 1)\n    let y = min(max(Int(point.y * scaleY), 0), rep.pixelsHigh - 1)\n    return rep.colorAt(x: x, y: y)?.usingColorSpace(.sRGB)\n  }\n\n  static func matches(_ a: NSColor?, _ b: NSColor?, tolerance: CGFloat) -> Bool {\n    guard\n      let a = a?.usingColorSpace(.sRGB),\n      let b = b?.usingColorSpace(.sRGB)\n    else { return false }\n    return abs(a.redComponent - b.redComponent) <= tolerance\n      && abs(a.greenComponent - b.greenComponent) <= tolerance\n      && abs(a.blueComponent - b.blueComponent) <= tolerance\n  }\n}\n```\n\nMechanism to reuse verbatim: `NSHostingView(rootView:)` \u2192 set `.frame` \u2192 `layoutSubtreeIfNeeded()`\n\u2192 `bitmapImageRepForCachingDisplay(in:)` + `cacheDisplay(in:to:)`. **No window, no NSWindow, no\nwindow server.** Color scheme is forced with `.environment(\\.colorScheme, .light)` on the root view\n(not `preferredColorScheme`). File imports are `AppKit`, `SwiftUI`, `XCTest`, `@testable import Nota`\n(`RecordingPaneTests.swift:1-5`).\n\nSecond consumer of the probe, worth copying as a pattern (`RecordingPaneTests.swift:216-260+`,\n`testStopIsAFillAndNotAMaterialThatTheBackdropCanChange`): it renders the same control over\n`Color.white` and `Color.black` inside a `ZStack`, samples one interior point with\n`RenderProbe.color(_:at:)`, and compares with `RenderProbe.matches(_:_:tolerance:)` \u2014 the way it\nproves a fill is not a material without being able to set `accessibilityReduceTransparency` (a\nread-only environment value).\n\n## 2. Actor + framework\n\n- Framework is **XCTest** everywhere. `grep -rln \"import Testing\"` across `macos/` returns nothing \u2014\n  swift-testing is not in use.\n- `@MainActor` is applied per-class, not globally. `RecordingPaneTests.swift:12` and\n  `RecordingAccentTests.swift:12` are both `@MainActor final class \u2026: XCTestCase`, and `RenderProbe`\n  itself is `@MainActor enum` (`RecordingPaneTests.swift:765`). Any new pixel test MUST be in a\n  `@MainActor` class or it cannot call `RenderProbe`.\n- Non-view suites deliberately have no annotation \u2014 `FieldEngineTests.swift:14` is a bare\n  `final class FieldEngineTests: XCTestCase`, as are `LiveMeetingViewTests`, `HistoryStatusTests`,\n  `SummaryDrawerTests`, etc. `@MainActor` appears only where AppKit/model state requires it\n  (`DeferredPresentationTests`, `BackgroundProcessingTests`, `LiveSessionLifecycleTests`,\n  `EnrichmentControllerTests`, `RecordingAccentTests`, `RecordingPaneTests`).\n\n## 3. Target layout\n\n`/Users/xiafawu/Developer/Nota/macos/project.yml` (XcodeGen; 127 lines). There is no per-file source\nlist \u2014 targets take **whole directories**, so a new `.swift` file dropped in\n`macos/Nota/UI/Tests/` is automatically in `NotaUITests` with no project.yml edit. The two test\ntargets, quoted (`project.yml:90-113`):\n\n```yaml\n  NotaDictationTests:\n    type: bundle.unit-test\n    platform: macOS\n    deploymentTarget: \"26.0\"\n    sources:\n      - path: Nota/Dictation/Tests\n    dependencies:\n      - target: Nota\n    settings:\n      base:\n        PRODUCT_BUNDLE_IDENTIFIER: com.xiafawu.nota.dictation-tests\n        GENERATE_INFOPLIST_FILE: YES\n  NotaUITests:\n    type: bundle.unit-test\n    platform: macOS\n    deploymentTarget: \"26.0\"\n    sources:\n      - path: Nota/UI/Tests\n    dependencies:\n      - target: Nota\n    settings:\n      base:\n        PRODUCT_BUNDLE_IDENTIFIER: com.xiafawu.nota.ui-tests\n        GENERATE_INFOPLIST_FILE: YES\n```\n\nAnd the app target excludes both test dirs from itself (`project.yml:36-42`):\n\n```yaml\n    sources:\n      - path: Nota\n        excludes:\n          - \"Info.plist\"\n          - \"Dictation/Tests\"\n          - \"UI/Tests\"\n          - \"Assets/.gitignore\"\n          - \"Assets/AppIcon.iconset\"\n          - \"Assets/AppIcon.svg\"\n          - \"Assets/AppIcon.icon\"\n```\n\nScheme runs both (`project.yml:120-123`):\n\n```yaml\n    test:\n      targets:\n        - name: NotaDictationTests\n        - name: NotaUITests\n```\n\nSo: **every** `.swift` under `/Users/xiafawu/Developer/Nota/macos/Nota/UI/Tests` belongs to\n`NotaUITests` \u2014 `BackgroundProcessingTests, BackgroundSummaryProcessTests, DeferredPresentationTests,\nDictionaryBulkImportTests, EnrichmentTests, FieldEngineTests, HistoryDrawerSearchTests,\nHistoryStatusTests, HomeDashboardStateTests, LiveMeetingViewTests, LiveSessionLifecycleTests,\nLiveSessionPersistenceTests, MainPolishTests, ModelNamespaceTests, RecordingAccentTests,\nRecordingPaneTests, RecordingStorageTests, SummaryDrawerTests, SummaryRailDismissalTests,\nUsageStatsProviderTests` plus the `Fixtures/` dir. Everything under `Nota/Dictation/Tests` (26 files:\n`HUDStyleTests, HUDPillSizingTests, DictationReviewTests, \u2026`) belongs to `NotaDictationTests`.\nBoth use `@testable import Nota` and are unhosted unit-test bundles.\n\n## 4. Existing tests that build a SwiftUI View and assert on it with no window server\n\nFour families, all usable as precedent:\n\n1. **`fittingSize` after `layoutSubtreeIfNeeded()`** \u2014 `RecordingAccentTests.swift:158-164`:\n   ```swift\n   private static func laidOutWidth(elapsed: TimeInterval, base: CGFloat) -> CGFloat {\n     let view = NSHostingView(rootView: SessionTimer(elapsed: elapsed, base: base))\n     view.layoutSubtreeIfNeeded()\n     return view.fittingSize.width\n   }\n   ```\n   Used by `testThePlateHoldsStillForAWholeSession` (`:151`), which asserts\n   `Set(widths).count == 1` across a session's durations.\n2. **Same, at the pane level** \u2014 `RecordingPaneTests.swift:418-455`\n   (`testTheColumnTakesItsWidthAndTheTranscriptTakesTheRest`) hosts `SessionColumnContent(...)` and\n   asserts `column.fittingSize.width == RecordingPaneMetrics.columnWidth` with `accuracy: 0.5`; and\n   `:465`, `:488`, `:516` do the same for column height vs `Metrics.windowMinHeight` and for\n   `SessionStripView`. Its doc comment records a real trap worth honoring: an earlier version wrapped\n   the HStack in `.frame(width: 1000)` and asserted `fittingSize.width == 1000` \u2014 **the frame fixed\n   the number under test**, so it was a tautology. Measure something the content must satisfy.\n3. **Pixel scan** \u2014 the `RenderProbe` pair above (`:725`, `:747`) and\n   `testStopIsAFillAndNotAMaterialThatTheBackdropCanChange` (`:216`).\n4. **HUD fitting-size baselines** in the other target \u2014\n   `/Users/xiafawu/Developer/Nota/macos/Nota/Dictation/Tests/HUDStyleTests.swift:262,269,298,364,472`\n   with `private static func fittingSize(_ view: NSHostingView<some View>) -> CGSize` at `:306`, and\n   `HUDPillSizingTests.swift:57`.\n\n`FieldEngineTests.swift` (422 lines) is the deliberate counter-example and states the house rule in\nits header: it asserts on the `[Float]` buffer (`FieldSimulation.step(dt:)`, `sim.color(x:y:)`,\n`FieldColor.mix`, `GroundInk.contrast`, `GroundInk.Tier.minimumContrast`, `GroundPalette.all`) with\n**no** SwiftUI/CoreGraphics at all \u2014 \"that is the point of keeping the simulation free of\nCoreGraphics and SwiftUI \u2026 a screenshot cannot answer it\". It is plain `final class \u2026: XCTestCase`\nwith only `import XCTest` + `@testable import Nota`. So the codebase's split is: numeric invariants \u2192\npure-type tests; \"is this on screen / how big is it\" \u2192 `NSHostingView`; \"what colour is on screen\" \u2192\n`RenderProbe`. Constants referenced from `.claude/design-2026-08-09/` prototypes and\n`.claude/checkpoint-2026-08-09.md`.\n"
}